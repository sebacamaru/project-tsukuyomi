{
  "name": "project-tsukuyomi",
  "private": true,
  "type": "module",
  "scripts": {
    "frontend": "cd frontend && bun run dev",
    "backend": "cd backend && bun ./src/server.js",
    "docker": "cd backend && docker-compose up -d",
    "dev": "concurrently \"npm:docker\" \"npm:backend\"",
    "build": "cd frontend && npm run build && cd .. && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
